<view class="section">
  <input bindinput="bindInput" placeholder="例如：商场， 医院，车站等..." focus="true" />
</view>
<view bindtouchstart="bindSearch" data-keywords="{{i.name}}" class="text_box" wx:for="{{tips}}" wx:for-item="i">
  {{i.name}}
</view>